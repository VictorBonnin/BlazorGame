@page "/landing" 
@inject NavigationManager Navigation

<!-- Le reste de ton design reste identique -->
<div class="container text-center mt-5">
    <h1 class="display-3">Bienvenue Aventurier !</h1>
    <p class="lead">Prêt à affronter le donjon ?</p>
    
    <div class="mt-4">
        <!-- Bouton qui redirige vers le login OIDC -->
        <button class="btn btn-lg btn-primary" @onclick="NavigateToLogin">
            Se connecter avec Keycloak
        </button>
    </div>
</div>

@code {
    private void NavigateToLogin()
    {
        // On redirige vers la page de login qui gère l'OIDC
        Navigation.NavigateTo("authentication/login");
    }
}